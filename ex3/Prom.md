-figma, css 클래스 이름
-figma 베리어블, figma 컴포넌트, css 컴포넌트, scss 변수명, 테일윈드와 부트스트랩 클래스명과 겹치지 않는 이름
-한 번 지정한 이름은 프로젝트 종료 때까지 반복적으로 사용할 예정
-최대 6글자 작성, 숫자 활용, 언더바(_)사용X, 대시(-)사용, 대문자 사용X -css에서 속성 1개 이상일 경우 figma 컴포넌트 이름과 동일
-css에서 속성 1개일 경우 figma 베리어블 이름과 동일
-코드 입력시 직관적인 이름, 5개씩 출력
-출력시 이전 데이터 전부 함께 작성


*풀이 조건
1.AI를 적극 활용한다
2.AI코드를 복사해서 사용할 수 없다
3.클래스 이름은 이름 짓기 채팅에서 작명 후 사용


*반응형 조건
1.1080이하에서 가로 레이아웃이 세로 레아아웃으로 변형
2.각 상자의 너비는 1440px 상속
3.각 상자의 높이는 300px로 변환


*해결 문제
1.가로 스크롤 해결(max)
2.flex-item 줄바꿈
3.줄바꿈 되었을때 생기는 문제 해결
ex)높이가 사라짐... 등...